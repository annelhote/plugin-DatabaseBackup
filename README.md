# plugin-DatabaseBackup
Omeka Plugin to backup the entire database into an SQL file and download it.

# Limitation
It seems it doesn't work for MySQL Server berfore version 5.0.77 because of the "SHOW CREATE TABLE" query.